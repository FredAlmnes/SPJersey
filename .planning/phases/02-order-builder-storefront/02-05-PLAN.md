---
phase: 02-order-builder-storefront
plan: 05
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - components/storefront/order-summary.tsx
  - components/storefront/cart-item-card.tsx
  - components/storefront/cart-panel.tsx
autonomous: true
requirements: [PROD-05]
must_haves:
  truths:
    - "The order summary shows live quantity, current unit price, and total in NOK, recomputed from config/pricing-tiers.ts on every cart change (PROD-05)"
    - "Adding more jerseys lowers the per-unit price via the whole-order bundle tiers (1=350, 2=320, 3+=290 kr) with a 'Pakkerabatt aktivert' note when the tier improves"
    - "Empty cart shows the UI-SPEC empty-state heading/body; cart is visible from first load as a persistent panel, not a separate cart page (D-13, D-16)"
    - "Each cart item card offers 'Rediger' (edit) and 'Fjern' (remove) with a 5-second inline 'Angre' undo instead of a confirmation dialog"
  artifacts:
    - path: "components/storefront/order-summary.tsx"
      provides: "live price summary derived from cart length"
      exports: ["OrderSummary"]
    - path: "components/storefront/cart-item-card.tsx"
      provides: "single cart line with edit/remove actions"
      exports: ["CartItemCard"]
    - path: "components/storefront/cart-panel.tsx"
      provides: "sticky desktop / fixed-bottom mobile cart panel with undo handling"
      exports: ["CartPanel"]
  key_links:
    - from: "components/storefront/order-summary.tsx"
      to: "config/pricing-tiers.ts"
      via: "getUnitPriceOre/getOrderTotalOre(cart.length)"
      pattern: "getOrderTotalOre"
    - from: "components/storefront/cart-panel.tsx"
      to: "lib/cart/cart-context.tsx"
      via: "useCart / useCartDispatch"
      pattern: "useCart"
---

<objective>
Build the persistent cart panel and its parts: the live order summary (derives price from cart length via Phase 1's pricing functions - never stores a price), the individual cart item card (edit/remove actions), and the responsive cart panel (sticky side panel on desktop, fixed bottom bar on mobile) that owns the 5-second undo-on-remove affordance. Reads Plan 01's cart Context; runs in parallel with Plans 03 and 04.

Purpose: Delivers the live order summary + bundle discount display (PROD-05) and the cart management surface (D-11..D-13, D-16). The "Rediger" action exposes an onEditItem callback that Plan 06 wires to the configurator's draft.
Output: components/storefront/{order-summary,cart-item-card,cart-panel}.tsx.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-order-builder-storefront/02-RESEARCH.md
@.planning/phases/02-order-builder-storefront/02-PATTERNS.md
@.planning/phases/02-order-builder-storefront/02-UI-SPEC.md
@config/pricing-tiers.ts

<interfaces>
<!-- Contracts this plan CREATES. FROM Plan 01: useCart/useCartDispatch from @/lib/cart/cart-context; CartItem from @/lib/cart/cart-types. FROM Phase 1: getUnitPriceOre/getOrderTotalOre from @/config/pricing-tiers. -->

components/storefront/order-summary.tsx:
  function OrderSummary()   // "use client"; useCart(); derives price from cart.length; empty-state copy

components/storefront/cart-item-card.tsx:
  interface CartItemCardProps {
    item: CartItem;
    unitPriceOre: number;                 // passed in from panel (recomputed for whole cart)
    onEdit: (item: CartItem) => void;
    onRemove: (item: CartItem) => void;
  }
  function CartItemCard(props)   // "use client"; shows config + unit price + Rediger/Fjern

components/storefront/cart-panel.tsx:
  interface CartPanelProps { onEditItem: (item: CartItem) => void }
  function CartPanel(props)   // "use client"; useCart()+useCartDispatch(); owns 5s undo state
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Live order summary (price derived from cart length)</name>
  <files>components/storefront/order-summary.tsx</files>
  <read_first>
    - config/pricing-tiers.ts (getUnitPriceOre/getOrderTotalOre - the ONLY price source)
    - lib/cart/cart-context.tsx (useCart - from Plan 01)
    - .planning/phases/02-order-builder-storefront/02-RESEARCH.md (Pattern 4 order summary; Pitfall 2 never store price on CartItem; formatOre helper: ore/100 + " kr")
    - .planning/phases/02-order-builder-storefront/02-PATTERNS.md (order-summary section - import pricing, never reimplement)
    - .planning/phases/02-order-builder-storefront/02-UI-SPEC.md (Order summary section: quantity/unit price/subtotal/total in "NNN kr"; "Pakkerabatt aktivert" note; empty-state copy; accent for total price)
  </read_first>
  <action>
    Create components/storefront/order-summary.tsx ("use client") exporting OrderSummary. Read cart via useCart(); quantity = cart.length. If quantity === 0, render the empty-state: heading "Handlekurven er tom" and body "Sett sammen din forste drakt i skjemaet for a komme i gang." (exact UI-SPEC copy). Otherwise compute unitPriceOre = getUnitPriceOre(quantity) and totalOre = getOrderTotalOre(quantity) fresh (do NOT read any stored price); render quantity x unit-price line, subtotal, and total formatted as "<ore/100> kr" (integer division is exact - tiers are whole hundreds). Show a short "Pakkerabatt aktivert" note near the total when quantity >= 2 (tier improved from the base 350). Total price uses the accent color (text-emerald-600 dark:text-emerald-500) and font-semibold. Wrap pricing calls in a try/catch or guard so a thrown pricing error shows the UI-SPEC fallback "Vi klarte ikke a beregne prisen. Prov a oppdatere siden." Header comment pointing to config/pricing-tiers.ts as the single source of truth.
  </action>
  <verify>
    <automated>grep -q "getOrderTotalOre" components/storefront/order-summary.tsx && grep -q "getUnitPriceOre" components/storefront/order-summary.tsx && ! grep -Eq "unitPriceOre[\"']?\s*[:=].*item|item\.(price|unitPrice)" components/storefront/order-summary.tsx && grep -q "Handlekurven er tom" components/storefront/order-summary.tsx && echo SUMMARY_OK</automated>
  </verify>
  <acceptance_criteria>
    - Price is always derived via getUnitPriceOre(cart.length)/getOrderTotalOre(cart.length); no price value is read off a CartItem
    - Empty state renders the exact UI-SPEC heading and body copy
    - Total shown in "NNN kr" format using the emerald accent; "Pakkerabatt aktivert" appears once quantity >= 2
    - A pricing failure path renders the UI-SPEC fallback error copy
    - npx tsc --noEmit clean for this file
  </acceptance_criteria>
  <done>The live order summary (PROD-05) reflects the whole-order bundle price on every cart change, sourced solely from Phase 1's pricing module.</done>
</task>

<task type="auto">
  <name>Task 2: Cart item card with edit/remove actions</name>
  <files>components/storefront/cart-item-card.tsx</files>
  <read_first>
    - lib/cart/cart-types.ts (CartItem shape - from Plan 01)
    - config/patches.ts (map patchIds -> labels for display)
    - .planning/phases/02-order-builder-storefront/02-PATTERNS.md (cart-item-card section - card surface convention)
    - .planning/phases/02-order-builder-storefront/02-UI-SPEC.md (Cart item cards section: show league/team + season, size, patch list or "Ingen", name/number if present, current unit price; "Rediger" pencil + "Fjern" trash destructive; 44px targets)
  </read_first>
  <action>
    Create components/storefront/cart-item-card.tsx ("use client") exporting CartItemCard with CartItemCardProps. Render one cart line on the repo card surface (rounded-lg border border-zinc-200 p-6 dark:border-zinc-800 dark:bg-zinc-950, adapt padding per UI-SPEC): league/team name + season, size, patch labels (resolve patchIds via PATCHES; show "Ingen" when only "ingen"), and name/number when present. Show the passed-in unitPriceOre formatted as "<ore/100> kr" (this is recomputed for the whole cart by the panel - the card does NOT compute it). Two icon buttons (lucide-react Pencil and Trash2, 44px targets): "Rediger" calls onEdit(item); "Fjern" (destructive color text-red-600 dark:text-red-400) calls onRemove(item). Header comment referencing D-12/PROD-05.
  </action>
  <verify>
    <automated>grep -q "onEdit" components/storefront/cart-item-card.tsx && grep -q "onRemove" components/storefront/cart-item-card.tsx && grep -q "lucide-react" components/storefront/cart-item-card.tsx && grep -q "text-red-600" components/storefront/cart-item-card.tsx && ! grep -q "font-medium" components/storefront/cart-item-card.tsx && echo CARD_OK</automated>
  </verify>
  <acceptance_criteria>
    - Card shows league/team, season, size, resolved patch labels (or "Ingen"), and name/number when present
    - Unit price is taken from the unitPriceOre prop, not computed in the card
    - "Rediger" calls onEdit(item); "Fjern" uses destructive color and calls onRemove(item); both meet 44px targets
    - No font-medium used
    - npx tsc --noEmit clean for this file
  </acceptance_criteria>
  <done>Each cart line (D-12) shows the full jersey config with recomputed unit price and exposes edit/remove callbacks.</done>
</task>

<task type="auto">
  <name>Task 3: Responsive cart panel with 5-second undo-on-remove</name>
  <files>components/storefront/cart-panel.tsx</files>
  <read_first>
    - lib/cart/cart-context.tsx (useCart/useCartDispatch - from Plan 01)
    - config/pricing-tiers.ts (getUnitPriceOre for the per-card unit price passed down)
    - components/storefront/order-summary.tsx (composed inside the panel - from Task 1)
    - components/storefront/cart-item-card.tsx (composed inside the panel - from Task 2)
    - .planning/phases/02-order-builder-storefront/02-PATTERNS.md (cart-panel section - sticky/fixed responsive classes)
    - .planning/phases/02-order-builder-storefront/02-UI-SPEC.md (Layout & responsive: desktop sticky top-6 ~360px zinc-50 panel; mobile fixed bottom-0 inset-x-0 bar; visible from first load D-16; item-count badge; "Angre" 5s undo, no confirm dialog)
  </read_first>
  <action>
    Create components/storefront/cart-panel.tsx ("use client") exporting CartPanel with CartPanelProps. Read cart via useCart() and dispatch via useCartDispatch(). Panel title "Din bestilling"; item-count badge in accent. Compute unitPriceOre = getUnitPriceOre(cart.length) once (when cart non-empty) and pass it to every CartItemCard so all items reflect the current whole-order tier (recomputed on every render). Map cart -> CartItemCard with onEdit={props.onEditItem} and onRemove={handleRemove}. handleRemove: dispatch {type:"remove", id}, stash the removed item in local useState, and show a transient inline "Angre" text-link (in place of that row) for 5 seconds via setTimeout; clicking "Angre" dispatches {type:"add", item} to restore it and clears the timer; after 5s the undo affordance disappears. Render OrderSummary at the bottom and a fully-enabled "Ga til betaling" button (accent, font-semibold) with a no-op onClick this phase (Phase 3 wires it; do NOT disable or add "coming soon"). Apply UI-SPEC responsive classes: desktop lg: sticky top-6 fixed-width panel with zinc-50/zinc-900 surface; mobile fixed bottom-0 inset-x-0 bar. Panel visible from first load even when empty (D-16). Header comment referencing D-13/D-16/PROD-05.
  </action>
  <verify>
    <automated>grep -q "useCart" components/storefront/cart-panel.tsx && grep -q "Angre" components/storefront/cart-panel.tsx && grep -q "setTimeout" components/storefront/cart-panel.tsx && grep -q "Ga til betaling\|til betaling" components/storefront/cart-panel.tsx && grep -q "sticky" components/storefront/cart-panel.tsx && ! grep -q "font-medium" components/storefront/cart-panel.tsx && echo PANEL_OK</automated>
  </verify>
  <acceptance_criteria>
    - Panel reads cart from Context, renders a CartItemCard per item with a whole-cart-recomputed unitPriceOre, and shows OrderSummary
    - Remove dispatches remove + shows a 5s inline "Angre" that re-adds the item on click (no confirmation dialog)
    - "Ga til betaling" is rendered enabled with a no-op handler (not disabled, no "coming soon")
    - Panel uses sticky desktop / fixed bottom mobile classes and is visible from first load (empty state included)
    - No font-medium used; npx tsc --noEmit clean for this file
  </acceptance_criteria>
  <done>The persistent, responsive cart panel (D-13/D-16) renders items with live bundle pricing (PROD-05), supports edit via callback, and offers low-stakes 5-second undo on remove.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| displayed total -> Phase 3 checkout | The price shown here is advisory; Phase 3 recomputes server-side |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-08 | Tampering | displayed order total | mitigate | Total is derived live from config/pricing-tiers.ts and never persisted on a CartItem, so no client-trusted price value exists for Phase 3 to accidentally consume (PAY-03 must recompute server-side) |
| T-02-09 | Denial of Service | pricing throw on unexpected quantity | mitigate | OrderSummary guards pricing calls and renders the UI-SPEC fallback copy instead of crashing the storefront if getUnitPriceOre throws |
</threat_model>

<verification>
- npx tsc --noEmit clean for all three files
- grep confirms order-summary imports getOrderTotalOre/getUnitPriceOre and stores no price on items
- grep confirms cart-panel implements the 5s "Angre" undo and an enabled "Ga til betaling" no-op button
</verification>

<success_criteria>
The live order summary (PROD-05, whole-order bundle pricing from Phase 1's module) and the persistent responsive cart panel (D-13/D-16) render correctly, expose an onEditItem callback for the configurator, and provide 5-second undo-on-remove - all with no client-stored price value.
</success_criteria>

<output>
Create `.planning/phases/02-order-builder-storefront/02-05-SUMMARY.md` when done.
</output>
