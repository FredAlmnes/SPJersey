---
phase: 01-foundation-data-model
plan: 05
type: execute
wave: 4
depends_on: ["01-04"]
files_modified: []
autonomous: false
requirements: [ADMIN-01]
must_haves:
  truths:
    - "A human confirms the full stack runs end-to-end (login → gated dashboard with a real DB read)"
    - "A human confirms unauthenticated /admin access redirects to /admin/login in a real browser"
  artifacts: []
  key_links: []
---

<objective>
Human verification of the walking skeleton: confirm the assembled stack (scaffold + Supabase + RLS + auth) runs end-to-end in a real browser — the login-redirect flow that the automated tests cannot fully exercise this phase (no E2E framework installed, per VALIDATION.md). This closes Phase 1's walking-skeleton "deployment / full-stack run" requirement.

Purpose: The automated auth integration test proves `signInWithPassword` wiring; only a human can confirm the browser redirect + rendered gated dashboard actually work together on the running stack.
Output: Owner sign-off (or a punch list of issues) on the end-to-end walking skeleton.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-data-model/SKELETON.md
@.planning/phases/01-foundation-data-model/01-VALIDATION.md
@README.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify the end-to-end walking skeleton in a browser</name>
  <action>Pause for the owner to run the full stack and confirm the login gate, redirect, and DB-backed dashboard work end-to-end. This is a verification-only checkpoint — no files are modified.</action>
  <what-built>The complete Phase 1 walking skeleton: a Next.js 16 app with a seeded single admin, a Supabase schema with RLS + idempotency guards, static catalog/pricing config, and a `getUser()`-gated `/admin` dashboard that reads real order data. All automated checks (build, typecheck, vitest unit + idempotency/RLS/auth integration tests) are green.</what-built>
  <how-to-verify>
    1. Start the stack per README "Run the full stack locally": `supabase start` (or hosted-project env), then `npx tsx scripts/seed-admin.ts` once, then `npm run dev`.
    2. In a browser, visit `http://localhost:3000/admin` while logged out → expect an automatic redirect to `/admin/login`.
    3. Log in with the seeded `ADMIN_EMAIL` / `ADMIN_PASSWORD` → expect to land on `/admin` and see the Norwegian dashboard showing an order count of 0.
    4. Confirm a wrong password shows an error and does NOT grant access.
    5. (Optional) Confirm the Vercel dev/preview deployment renders the same `/admin/login` gate if deployed.
  </how-to-verify>
  <verify>
    <human-check>Owner confirms redirect + gated dashboard + wrong-password rejection all behave as described.</human-check>
  </verify>
  <acceptance_criteria>
    - Unauthenticated `/admin` visibly redirects to `/admin/login`
    - Seeded credentials grant access to the `/admin` dashboard showing an order count
    - Wrong password is rejected
  </acceptance_criteria>
  <resume-signal>Type "approved" if the login gate, redirect, and dashboard read all work; otherwise describe what failed.</resume-signal>
  <done>Owner has confirmed the end-to-end walking skeleton works, or provided a punch list of issues.</done>
</task>

</tasks>

<verification>
Human confirms: unauthenticated `/admin` → redirect to `/admin/login`; seeded credentials → gated dashboard with real orders read; wrong password rejected.
</verification>

<success_criteria>
Owner has run the full stack and confirmed the end-to-end walking skeleton (login gate + redirect + real DB-backed dashboard) works, satisfying the walking-skeleton deployment/full-stack-run requirement and final confirmation of ADMIN-01.
</success_criteria>

<output>
Create `.planning/phases/01-foundation-data-model/01-05-SUMMARY.md` when done.
</output>
