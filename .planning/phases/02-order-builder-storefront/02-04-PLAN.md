---
phase: 02-order-builder-storefront
plan: 04
type: execute
wave: 2
depends_on: ["02-01", "02-02"]
files_modified:
  - components/storefront/patch-checkboxes.tsx
  - components/storefront/name-number-fields.tsx
autonomous: true
requirements: [PROD-03, PROD-04]
must_haves:
  truths:
    - "Customer can check patches from the fixed list, with 'Ingen' pre-selected by default and mutually exclusive with the others (PROD-03)"
    - "Customer can type an optional print name and number; invalid input shows a live inline error and a destructive-color border on the field (PROD-04, D-23)"
    - "Valid/empty name and number produce no error; validation only runs on non-empty input (D-22)"
  artifacts:
    - path: "components/storefront/patch-checkboxes.tsx"
      provides: "patch chip/checkbox group using togglePatch mutual-exclusivity"
      exports: ["PatchCheckboxes"]
    - path: "components/storefront/name-number-fields.tsx"
      provides: "live-validated print name/number inputs"
      exports: ["NameNumberFields"]
  key_links:
    - from: "components/storefront/patch-checkboxes.tsx"
      to: "lib/cart/patch-selection.ts"
      via: "togglePatch(value, clickedId)"
      pattern: "togglePatch"
    - from: "components/storefront/name-number-fields.tsx"
      to: "lib/validation/jersey-schema.ts"
      via: "jerseyNameSchema/jerseyNumberSchema safeParse"
      pattern: "safeParse"
---

<objective>
Build the two "print details" form components: the patch checkbox/chip group (reads Phase 1's PATCHES, enforces "Ingen" mutual exclusivity via Plan 01's togglePatch) and the live-validated print name/number inputs (uses Plan 02's Zod schemas for on-blur/on-change feedback per D-23). Both are controlled components the configurator form (Plan 06) composes.

Purpose: Delivers PROD-03 (patch selection incl. "Ingen") and PROD-04 (name/number with live validation) UI. Runs in parallel with Plans 03 and 05 (no shared files).
Output: components/storefront/{patch-checkboxes,name-number-fields}.tsx.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-order-builder-storefront/02-RESEARCH.md
@.planning/phases/02-order-builder-storefront/02-PATTERNS.md
@.planning/phases/02-order-builder-storefront/02-UI-SPEC.md
@config/patches.ts

<interfaces>
<!-- Contracts this plan CREATES (props). Consumed by configurator-form.tsx (Plan 06). -->
<!-- FROM Plan 01: togglePatch from @/lib/cart/patch-selection. FROM Plan 02: jerseyNameSchema/jerseyNumberSchema from @/lib/validation/jersey-schema. -->

components/storefront/patch-checkboxes.tsx:
  interface PatchCheckboxesProps { value: string[]; onChange: (patchIds: string[]) => void }
  function PatchCheckboxes(props)   // "use client"; renders PATCHES; uses togglePatch internally

components/storefront/name-number-fields.tsx:
  interface NameNumberFieldsProps {
    name: string; number: string;                 // draft strings (number kept as string in draft)
    onNameChange: (v: string) => void;
    onNumberChange: (v: string) => void;
  }
  function NameNumberFields(props)   // "use client"; shows live per-field Zod errors internally
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Patch checkbox/chip group with "Ingen" exclusivity</name>
  <files>components/storefront/patch-checkboxes.tsx</files>
  <read_first>
    - lib/cart/patch-selection.ts (togglePatch - from Plan 01)
    - config/patches.ts (PATCHES ids/labels including "ingen")
    - .planning/phases/02-order-builder-storefront/02-RESEARCH.md (Pitfall 6 single togglePatch handler, not per-checkbox handlers)
    - .planning/phases/02-order-builder-storefront/02-PATTERNS.md (patch-checkboxes section - accent-selected chip styling, no UI analog)
    - .planning/phases/02-order-builder-storefront/02-UI-SPEC.md (Patches: label "Merker (valgfritt)"; "Ingen" pre-checked default; accent = checked chip; 44px touch target)
  </read_first>
  <action>
    Create components/storefront/patch-checkboxes.tsx ("use client") exporting PatchCheckboxes with PatchCheckboxesProps. Render a "Merker (valgfritt)" label and map PATCHES from @/config/patches to a checkbox/chip control each (44px min touch target). Selected state (value.includes(patch.id)) uses the accent (bg-emerald-600 text-white dark:bg-emerald-500); unselected neutral (border-zinc-300). On click of any chip call onChange(togglePatch(value, patch.id)) - a single handler routing through the pure function (NOT independent per-checkbox state). Do NOT reimplement the exclusivity rule inline; import and use togglePatch. Note the parent seeds value with ["ingen"] as the default (documented for Plan 06); this component just renders whatever value it is given. Use font-semibold for checked emphasis, never font-medium. Header comment referencing PROD-03/D-09/D-10.
  </action>
  <verify>
    <automated>grep -q "togglePatch" components/storefront/patch-checkboxes.tsx && grep -q "PATCHES" components/storefront/patch-checkboxes.tsx && grep -q "emerald" components/storefront/patch-checkboxes.tsx && ! grep -q "font-medium" components/storefront/patch-checkboxes.tsx && echo PATCH_OK</automated>
  </verify>
  <acceptance_criteria>
    - Renders all PATCHES entries including "Ingen" from config/patches.ts (not hardcoded)
    - Toggling routes exclusively through togglePatch(value, id) via a single onChange call - no per-checkbox useState
    - Checked chips use the emerald accent; unchecked stay neutral; 44px touch targets
    - No font-medium used
    - npx tsc --noEmit clean for this file
  </acceptance_criteria>
  <done>Customer can select patches with correct "Ingen" mutual exclusivity (PROD-03), driven by the tested togglePatch function.</done>
</task>

<task type="auto">
  <name>Task 2: Live-validated print name/number inputs</name>
  <files>components/storefront/name-number-fields.tsx</files>
  <read_first>
    - lib/validation/jersey-schema.ts (jerseyNameSchema, jerseyNumberSchema - from Plan 02)
    - .planning/phases/02-order-builder-storefront/02-RESEARCH.md (Pitfall 4 treat number input as untrusted string; D-23 live on blur/change)
    - .planning/phases/02-order-builder-storefront/02-PATTERNS.md (name-number-fields section - labeled input + error text + dual signal red border)
    - .planning/phases/02-order-builder-storefront/02-UI-SPEC.md (Name/number validation section; labels "Navn pa trykk (valgfritt)"/"Nummer pa trykk (valgfritt)"; destructive color for error text + input border; 14px/400 error copy)
    - app/(admin)/admin/login/page.tsx (labeled input + conditional error paragraph convention)
  </read_first>
  <action>
    Create components/storefront/name-number-fields.tsx ("use client") exporting NameNumberFields with NameNumberFieldsProps. Two labeled fields: "Navn pa trykk (valgfritt)" (text input, value=name) and "Nummer pa trykk (valgfritt)" (text input treated as string, value=number - do NOT rely on type=number min/max alone per Pitfall 4). On change/blur (D-23), run jerseyNameSchema.safeParse(name) / jerseyNumberSchema.safeParse(number) and store a per-field error string in local useState; call onNameChange/onNumberChange to lift the raw string to the parent draft. When a field has an error, render the schema's message below it in destructive text (text-sm text-red-600 dark:text-red-400) AND apply a destructive border class to the input (border-red-600 dark:border-red-400 when error, else border-zinc-300 dark:border-zinc-700). Empty input shows no error (optional, D-22). Labels use regular weight (NO font-medium). Header comment referencing PROD-04/D-20..D-23.
  </action>
  <verify>
    <automated>grep -q "safeParse" components/storefront/name-number-fields.tsx && grep -q "jerseyNameSchema" components/storefront/name-number-fields.tsx && grep -q "border-red-600" components/storefront/name-number-fields.tsx && ! grep -q "font-medium" components/storefront/name-number-fields.tsx && echo NAMENUM_OK</automated>
  </verify>
  <acceptance_criteria>
    - Both fields are optional and validated live via jerseyNameSchema/jerseyNumberSchema safeParse on change/blur
    - Invalid input renders the schema error message AND a destructive-color input border; empty input shows no error
    - Number field is validated through the Zod schema, not only HTML min/max attributes
    - Labels use regular weight (no font-medium)
    - npx tsc --noEmit clean for this file
  </acceptance_criteria>
  <done>Customer can enter an optional print name and number with live validation feedback (PROD-04, D-23), backed by the tested Zod schemas.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| customer print input -> cart draft | Free-text name/number typed for the jersey print, later relayed to supplier (Phase 4) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-06 | Tampering | print name/number fields | mitigate | Both fields validated through the Plan 02 Zod schemas (letters/spaces/hyphens only; integer 0-99) on every change/blur - invalid values never enter the draft; React JSX escapes rendered values |
| T-02-07 | Tampering | patch selection state | mitigate | Selection routes only through the pure togglePatch function, so state can never reach an invalid combination (both "Ingen" and another patch, or empty) regardless of click ordering |
| T-02-V5b | — | client validation trust boundary | accept | Client-side only this phase; Phase 3 must re-validate server-side before DB write / supplier send (ASVS V5 defense-in-depth forward dependency) |
</threat_model>

<verification>
- npx tsc --noEmit clean for both component files
- grep confirms patch-checkboxes uses togglePatch and name-number-fields uses safeParse + a destructive border class
- grep confirms no font-medium in either file
</verification>

<success_criteria>
The patch group (PROD-03, correct "Ingen" exclusivity) and the print name/number fields (PROD-04, live validation with dual error signal) render as controlled components matching the UI-SPEC contract, wired to the tested Plan 01/02 logic.
</success_criteria>

<output>
Create `.planning/phases/02-order-builder-storefront/02-04-SUMMARY.md` when done.
</output>
