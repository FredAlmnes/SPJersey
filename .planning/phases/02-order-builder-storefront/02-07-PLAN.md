---
phase: 02-order-builder-storefront
plan: 07
type: execute
wave: 4
depends_on: ["02-06"]
files_modified: []
autonomous: false
requirements: [PROD-02, PROD-05, PROD-06, PROD-07]
must_haves:
  truths:
    - "A human confirms the full storefront flow works in the browser: configure -> add -> live total -> edit -> checkout CTA"
    - "The behaviors excluded from Option A automated tests (size selection, size-guide modal, live-summary visual update, checkout explainer) are visually verified (02-VALIDATION.md Manual-Only table)"
  artifacts: []
  key_links: []
---

<objective>
Human-verify the assembled storefront in a real browser. Option A (logic-only) testing per 02-VALIDATION.md deliberately excludes component-rendering tests for PROD-02 (size selection), PROD-06 (size-guide modal), the PROD-05 live-summary visual update, and PROD-07 (checkout explainer) - these are covered here by manual verification, plus a full happy-path walkthrough of the phase goal.

Purpose: Close the phase's manual-verification gap and confirm the end-to-end customer flow before `/gsd:verify-work`. No code changes.
Output: Human confirmation (or a list of issues to feed a gap-closure plan).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-order-builder-storefront/02-VALIDATION.md
@.planning/phases/02-order-builder-storefront/02-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ensure the app runs and the full suite is green</name>
  <files>none (verification-only)</files>
  <read_first>
    - .planning/phases/02-order-builder-storefront/02-VALIDATION.md (Manual-Only Verifications table; full-suite gate)
  </read_first>
  <action>
    Run `npm test` and confirm the full Vitest suite is green (all Phase 1 config tests + the Phase 2 lib tests). Run `npx next build` to confirm a clean production build (no duplicate-route error, no type errors). Then start `npm run dev` so the human can open the storefront at http://localhost:3000/. Report the dev URL and confirm the page loads without console errors.
  </action>
  <verify>
    <automated>npm test 2>&1 | tail -3 && npx next build 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` full suite passes
    - `npx next build` succeeds
    - Dev server serves the storefront at '/'
  </acceptance_criteria>
  <done>The app is verified-buildable and running locally, ready for human walkthrough.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human-verify the end-to-end storefront flow in a browser</name>
  <action>Pause for the owner to walk through the full storefront in a browser and confirm each step below. This is a verification-only checkpoint - no files are modified.</action>
  <what-built>
    The complete customer-facing storefront at '/': cascading league/team selection with read-only season, six-size selector, size-guide modal, patch group with "Ingen" default, live-validated name/number print fields, a persistent cart panel with live bundle-discounted total, in-place edit of cart items, 5-second undo on remove, and a post-payment checkout explainer with an enabled (no-op) "Ga til betaling" button.
  </what-built>
  <how-to-verify>
    1. Open http://localhost:3000/ - confirm the cart panel is visible from first load with the empty-state copy "Handlekurven er tom" (D-16).
    2. PROD-01: pick a league, then a team; confirm the team list matches the league and "Sesong: 2025/26" appears as read-only text. Also try "Landslag" and confirm national teams (incl. "Norge") appear.
    3. PROD-02 + PROD-06: click each of the six sizes S-3XL and confirm selection highlights in green; click "Se storrelsesguide" and confirm the modal opens with the S-3XL table + disclaimer, closes on Escape and on the "Lukk" button.
    4. PROD-03: confirm "Ingen" is pre-checked; check a real patch and confirm "Ingen" unchecks; check "Ingen" again and confirm the others clear.
    5. PROD-04: type an invalid name (e.g. "Name123") and a number > 99; confirm live inline errors + red borders; type a valid international name (e.g. "Muller") and confirm the error clears; leave both empty and confirm no error.
    6. PROD-05: click "Legg i handlekurv"; confirm the jersey appears in the cart and the total shows "350 kr". Add a second (total 640 kr / 320 each) and a third (total 870 kr / 290 each); confirm "Pakkerabatt aktivert" appears and unit prices recompute for ALL items.
    7. D-12: click "Rediger" on a cart item; confirm the form reopens pre-filled, change the size, click "Oppdater", and confirm the item updates in place (no duplicate added).
    8. Remove an item and confirm a 5-second "Angre" undo link appears in its place; click it and confirm the item returns.
    9. PROD-07: confirm the checkout explainer text ("Etter betaling far du en ordrebekreftelse...") appears near the "Ga til betaling" button, and that the button is enabled (clicking it does nothing this phase - no error, no navigation).
    10. Resize to mobile width and confirm the cart collapses to a fixed bottom bar that does not overlap the form's CTA.
  </how-to-verify>
  <verify>
    <human-check>Owner confirms all 10 walkthrough steps behave as described (PROD-01..PROD-07, D-12, D-16, undo, responsive).</human-check>
  </verify>
  <acceptance_criteria>
    - Full configure -> add -> live-total -> edit -> checkout-CTA flow works end-to-end
    - Size selection, size-guide modal, live-summary visual update, and checkout explainer (the Option A manual-only rows) all pass
    - Bundle pricing shows 350/640/870 kr at qty 1/2/3 and "Pakkerabatt aktivert" appears
  </acceptance_criteria>
  <resume-signal>Type "approved" if all checks pass, or describe any issues (they will be routed to a gap-closure plan).</resume-signal>
  <done>Owner has confirmed the end-to-end storefront flow and the four manual-only behaviors, or provided a punch list of issues for gap closure.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| n/a | Verification-only plan; no new code or trust boundary introduced |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-13 | n/a | manual verification plan | accept | No code changes; this plan only observes the already-mitigated Wave 1-3 behavior |
</threat_model>

<verification>
- Automated: npm test + npx next build green (Task 1)
- Manual: all 10 human-verify steps pass (covers PROD-02/05/06/07 manual-only rows from 02-VALIDATION.md + full happy path)
</verification>

<success_criteria>
A human has confirmed the end-to-end storefront flow and the four manual-only behaviors (size selection, size-guide modal, live-summary visual update, checkout explainer) work correctly in the browser, or has logged issues for gap closure.
</success_criteria>

<output>
Create `.planning/phases/02-order-builder-storefront/02-07-SUMMARY.md` when done.
</output>
