---
phase: 02-order-builder-storefront
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/validation/jersey-schema.ts
  - lib/validation/jersey-schema.test.ts
autonomous: true
requirements: [PROD-04]
must_haves:
  truths:
    - "jerseyNameSchema accepts international letters (Müller, José, Şahin, Åge-Sørensen), spaces, hyphens; rejects digits/symbols; enforces max 12 chars (D-20)"
    - "jerseyNumberSchema accepts integer strings 0–99 only; rejects decimals, negatives, >99, and non-numeric (D-21)"
    - "Both schemas treat empty string as valid — name and number are optional (D-22)"
    - "Error messages exactly match the UI-SPEC Copywriting Contract strings"
  artifacts:
    - path: "lib/validation/jersey-schema.ts"
      provides: "Zod schemas for the print name/number fields — the validation contract consumed by the name-number-fields component (client) and re-usable server-side in Phase 3"
      exports: ["jerseyNameSchema", "jerseyNumberSchema"]
  key_links:
    - from: "lib/validation/jersey-schema.ts"
      to: "zod"
      via: "z.string().regex(/^[\\p{L}\\s-]*$/u)"
      pattern: "\\\\p\\{L\\}"
---

<objective>
Create the Zod validation schemas for the jersey print name (max 12 chars, Unicode letters + space + hyphen) and print number (integer 0–99), both optional, with error copy locked to the UI-SPEC Copywriting Contract. This is the tested validation contract for PROD-04 that the Wave 2 `name-number-fields` component consumes for live on-blur/on-change feedback (D-23), and that Phase 3 re-uses for defense-in-depth server-side validation.

Purpose: PROD-04 requires validation on name/number with correct handling of international letters. Option A testing (02-VALIDATION.md) makes this schema a pure-function test target. Runs in parallel with Plan 01 (no shared files).
Output: `lib/validation/jersey-schema.ts` + test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-order-builder-storefront/02-CONTEXT.md
@.planning/phases/02-order-builder-storefront/02-RESEARCH.md
@.planning/phases/02-order-builder-storefront/02-PATTERNS.md
@.planning/phases/02-order-builder-storefront/02-UI-SPEC.md

<interfaces>
<!-- Contract this plan CREATES. Consumed by components/storefront/name-number-fields.tsx (Plan 04). -->
lib/validation/jersey-schema.ts:
  const jerseyNameSchema     // z schema: trim, max 12, /^[\p{L}\s-]*$/u, optional, "" allowed
  const jerseyNumberSchema   // z union: "" | digit-string 1–2 chars refined to 0..99, optional
  // Both used via .safeParse(value) → { success, error } for live per-field feedback.

Locked error strings (UI-SPEC Copywriting Contract — use verbatim):
  name:   "Kan bare inneholde bokstaver, mellomrom og bindestrek (maks 12 tegn)."
  number: "Må være et helt tall mellom 0 og 99."
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Zod name/number schemas (with tests)</name>
  <files>lib/validation/jersey-schema.ts, lib/validation/jersey-schema.test.ts</files>
  <behavior>
    - jerseyNameSchema.safeParse("") .success === true (optional/empty allowed, D-22)
    - "Müller", "José", "Şahin", "Åge-Sørensen", "Ole Gunnar" → success true (Unicode letters + space + hyphen)
    - "Name123" → success false (digits rejected)
    - "Toolongname12" (13 chars) → success false (max 12, D-20)
    - jerseyNumberSchema.safeParse("") .success === true (optional)
    - "0", "7", "99" → success true ; "100" → false ; "-1" → false ; "1.5" → false ; "abc" → false
    - On failure, the emitted issue message equals the exact UI-SPEC string for that field
  </behavior>
  <read_first>
    - .planning/phases/02-order-builder-storefront/02-RESEARCH.md (Code Examples §Zod schema — verbatim, locally verified against zod 4.4.3; note on regex-then-coerce vs z.coerce.number pitfall)
    - .planning/phases/02-order-builder-storefront/02-PATTERNS.md (jersey-schema.ts section — no repo analog, use RESEARCH example; error strings must match UI-SPEC)
    - .planning/phases/02-order-builder-storefront/02-UI-SPEC.md (Copywriting Contract — exact name/number error strings)
    - config/patches.test.ts (simplest Vitest test-shape analog)
  </read_first>
  <action>
    Create `lib/validation/jersey-schema.ts` importing `z` from `zod`, exporting `jerseyNameSchema` and `jerseyNumberSchema` per the RESEARCH Code Example verbatim: name = `z.string().trim().max(12, <name-error>).regex(/^[\p{L}\s-]*$/u, <name-error>).optional().or(z.literal(""))`; number = `z.union([z.literal(""), z.string().regex(/^\d{1,2}$/, <number-error>).refine(v => Number(v) >= 0 && Number(v) <= 99, <number-error>)]).optional()`. Use the exact UI-SPEC error strings shown in `<interfaces>`. Do NOT hardcode `æøåÆØÅ` — the `\p{L}` Unicode property escape (with the `u` flag) is required (Pitfall 3). Do NOT use `z.coerce.number()` on raw input (Pitfall 4 — regex-then-refine is stricter). Add the `// lib/validation/jersey-schema.ts` header comment referencing D-20/D-21/D-22.
    Create `lib/validation/jersey-schema.test.ts` (Vitest, relative `./jersey-schema` import) asserting every `<behavior>` bullet, including at least one issue-message equality check against the exact UI-SPEC string.
  </action>
  <verify>
    <automated>npx vitest run lib/validation/jersey-schema.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `jersey-schema.ts` regex uses `\p{L}` with the `u` flag; grep confirms it contains no literal `æøå` character class
    - Schema uses no `z.coerce.number(` call
    - Test proves "Müller"/"José"/"Şahin" pass and "Name123"/13-char string fail for name; "0"/"99" pass and "100"/"-1"/"1.5" fail for number; "" passes both
    - At least one test asserts the exact error message string from the UI-SPEC contract
    - `npx vitest run lib/validation/jersey-schema.test.ts` passes
  </acceptance_criteria>
  <done>The print name/number validation contract (PROD-04) exists as tested Zod schemas with the exact locked Norwegian error copy, ready for live client feedback and Phase 3 server re-validation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| customer input → print field | Free-text name/number the customer types for the jersey print |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-02 | Tampering / Info Disclosure | print name field (downstream WhatsApp/admin render) | mitigate | Zod restricts name to letters/spaces/hyphens only (no `<`, `>`, quotes), shrinking injection surface for Phase 4 WhatsApp message and Phase 5 admin panel; React JSX escaping covers on-page render this phase |
| T-02-03 | Tampering | print number field | mitigate | Regex-then-refine (0–99 integer) rather than `z.coerce.number()`, blocking `"1e2"`/`"0x10"`/decimals from reaching cart state and, later, the supplier message (a wrong printed number is a real production error) |
| T-02-V5 | — | client-side validation trust | accept | This phase's Zod validation is a UX convenience only; ASVS V5 defense-in-depth requires Phase 3 to re-validate server-side before any DB write or supplier send — noted as a forward dependency, not fixable here |
</threat_model>

<verification>
- `npx vitest run lib/validation/jersey-schema.test.ts` green
- `npx tsc --noEmit` clean
- grep confirms `\p{L}` present and no literal Norwegian-only character class
</verification>

<success_criteria>
Name/number print validation (PROD-04) exists as tested Zod schemas: international letters accepted, invalid input rejected, D-20/D-21/D-22 bounds enforced, and error copy matching the UI-SPEC contract exactly.
</success_criteria>

<output>
Create `.planning/phases/02-order-builder-storefront/02-02-SUMMARY.md` when done.
</output>
