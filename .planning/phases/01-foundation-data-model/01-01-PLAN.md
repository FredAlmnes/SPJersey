---
phase: 01-foundation-data-model
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - tsconfig.json
  - next.config.ts
  - eslint.config.mjs
  - postcss.config.mjs
  - app/layout.tsx
  - app/page.tsx
  - app/globals.css
  - vitest.config.ts
  - .env.local
  - .env.example
  - .gitignore
  - supabase/config.toml
  - lib/supabase/client.ts
  - lib/supabase/server.ts
  - lib/supabase/service-role.ts
autonomous: true
requirements: [ADMIN-01]
must_haves:
  truths:
    - "The Next.js 16 app builds and lints with zero errors"
    - "Vitest runs and reports (even with zero tests) via `npx vitest run`"
    - "A local Supabase project is initialised (supabase/config.toml exists)"
    - "Three distinct Supabase client factories exist, each scoped to its trust boundary"
  artifacts:
    - path: "package.json"
      provides: "Next.js 16 + Supabase + Vitest dependency manifest"
      contains: "@supabase/ssr"
    - path: "lib/supabase/server.ts"
      provides: "createServerClient cookie-bound factory"
      exports: ["createClient"]
    - path: "lib/supabase/client.ts"
      provides: "createBrowserClient factory"
      exports: ["createClient"]
    - path: "lib/supabase/service-role.ts"
      provides: "service-role client, server-only guarded"
      exports: ["createServiceRoleClient"]
    - path: "supabase/config.toml"
      provides: "local Supabase stack config"
      contains: "project_id"
  key_links:
    - from: "lib/supabase/service-role.ts"
      to: "process.env.SUPABASE_SERVICE_ROLE_KEY"
      via: "server-only guarded env read"
      pattern: "SUPABASE_SERVICE_ROLE_KEY"
---

<objective>
Scaffold the Next.js 16 App Router project (the "application itself" as the walking-skeleton feature), install the locked dependency set, initialise a local Supabase project, install the Vitest test runner, and create the three trust-boundary-scoped Supabase client factories every later slice depends on.

Purpose: Establish the foundation Wave 2/3 build on — schema, catalog, and auth all require the scaffold, the Supabase clients, and the test runner to exist first.
Output: A buildable, lintable, testable Next.js 16 + Supabase project skeleton with three Supabase client factories.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-data-model/SKELETON.md
@.planning/phases/01-foundation-data-model/01-RESEARCH.md
@.planning/phases/01-foundation-data-model/01-PATTERNS.md
@CLAUDE.md

<interfaces>
<!-- Verified package versions (RESEARCH.md, npm-checked 2026-07-07). Use these exact versions. -->
next@16.2.10 · @supabase/supabase-js@2.110.0 · @supabase/ssr@0.12.0 · typescript@6.0.3 · tailwindcss@4.3.2 · zod@4.4.3
Dev: supabase (CLI), vitest, server-only

<!-- Factory contracts downstream plans import (do NOT change these signatures): -->
lib/supabase/server.ts    → export async function createClient(): Promise<SupabaseClient>   // cookie-bound, createServerClient
lib/supabase/client.ts    → export function createClient(): SupabaseClient                   // createBrowserClient
lib/supabase/service-role.ts → export function createServiceRoleClient(): SupabaseClient      // 'server-only', SERVICE_ROLE_KEY
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Next.js 16 app + install dependencies + init Supabase + Vitest</name>
  <files>package.json, package-lock.json, tsconfig.json, next.config.ts, eslint.config.mjs, postcss.config.mjs, app/layout.tsx, app/page.tsx, app/globals.css, vitest.config.ts, .env.local, .env.example, .gitignore, supabase/config.toml</files>
  <read_first>
    - CLAUDE.md (locked stack: Next.js 16 App Router only, Tailwind 4, TypeScript, Supabase; forbids Pages Router and @supabase/auth-helpers-nextjs)
    - .planning/phases/01-foundation-data-model/01-RESEARCH.md ("Installation" block lines 120-128, "Recommended Project Structure" lines 193-222, "Environment Availability" lines 445-457 for the Docker/hosted-Supabase fallback)
    - .planning/phases/01-foundation-data-model/SKELETON.md (Architectural Decisions table)
  </read_first>
  <action>
    Run `npx create-next-app@latest . --typescript --tailwind --app --turbopack --eslint --import-alias "@/*"` in the repo root (the directory already contains CLAUDE.md, .planning/, .claude/, .mcp.json — accept scaffolding into a non-empty dir; do NOT delete those). Pin `next` to 16.2.10 if the scaffolder installs a different major.
    Install runtime deps: `@supabase/supabase-js@2.110.0`, `@supabase/ssr@0.12.0`, `zod@4.4.3`, `server-only`. Install dev deps: `supabase` (CLI), `vitest`.
    Run `npx supabase init` to create `supabase/config.toml`. Do NOT run `supabase start` yet — if Docker is unavailable, Plan 02 documents the hosted-Supabase fallback per RESEARCH.md Environment Availability.
    Create `vitest.config.ts` with the Node environment and a `test.include` glob covering `config/**/*.test.ts` and `tests/**/*.test.ts`.
    Create `.env.example` and `.env.local` with exactly these keys (values empty/placeholder in .env.example): `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`, `SUPABASE_SERVICE_ROLE_KEY`, `ADMIN_EMAIL`, `ADMIN_PASSWORD`. The service-role key MUST NOT carry a `NEXT_PUBLIC_` prefix (RESEARCH.md Pitfall 5).
    Ensure `.gitignore` ignores `.env.local` and `.env*.local` (keep `.env.example` tracked).
    Leave the default `app/page.tsx` as a minimal public placeholder landing page (Norwegian text is fine, not required this task) and keep `app/layout.tsx`.
  </action>
  <verify>
    <automated>npx next build && npx eslint . && npx vitest run --passWithNoTests && test -f supabase/config.toml && echo OK</automated>
    <latency-note>EXEMPT from VALIDATION.md's 15s max-feedback-latency target. That target governs the fast per-task test loop used during iterative development; this is a one-time scaffold/setup gate (`next build` + `eslint` + `vitest`) that runs once when the project is first stood up, not a repeated inner-loop check. It is expected to exceed 15s and does not represent a Nyquist sampling violation. Downstream tasks (Plans 02/04) use single-file `npx vitest run <file>` commands that stay inside the 15s target.</latency-note>
  </verify>
  <acceptance_criteria>
    - `npx next build` exits 0 (App Router build succeeds)
    - `package.json` lists `next` (16.x), `@supabase/supabase-js`, `@supabase/ssr`, `zod`, `server-only`, and devDeps `supabase` + `vitest`
    - `supabase/config.toml` exists and contains a `project_id` key
    - `.env.example` contains all five keys and NO key named `NEXT_PUBLIC_SUPABASE_SERVICE_ROLE_KEY`
    - `.gitignore` contains `.env.local`
    - `npx vitest run --passWithNoTests` exits 0
  </acceptance_criteria>
  <done>Buildable, lintable Next.js 16 App Router scaffold with Supabase + Vitest installed and a local Supabase project initialised.</done>
</task>

<task type="auto">
  <name>Task 2: Create three trust-boundary Supabase client factories</name>
  <files>lib/supabase/client.ts, lib/supabase/server.ts, lib/supabase/service-role.ts</files>
  <read_first>
    - .planning/phases/01-foundation-data-model/01-PATTERNS.md (verbatim excerpts for server.ts lines 37-62, client.ts lines 73-82, service-role.ts lines 93-104, plus the "server-only import guard" shared pattern lines 299-301)
    - .planning/phases/01-foundation-data-model/01-RESEARCH.md (Pitfall 5 lines 357-361 — service-role key exposure)
    - CLAUDE.md (forbids @supabase/auth-helpers-nextjs; @supabase/ssr only)
  </read_first>
  <action>
    Create `lib/supabase/server.ts` exporting `async function createClient()` using `createServerClient` from `@supabase/ssr` with the `cookies()` getAll/setAll cookie bridge (try/catch swallow on setAll for Server Component calls) — implement per the PATTERNS.md verbatim excerpt. Reads `NEXT_PUBLIC_SUPABASE_URL` and `NEXT_PUBLIC_SUPABASE_ANON_KEY`.
    Create `lib/supabase/client.ts` exporting `function createClient()` using `createBrowserClient` from `@supabase/ssr` with the same two public env vars. Do NOT add `server-only` here.
    Create `lib/supabase/service-role.ts` exporting `function createServiceRoleClient()` using `createClient` from `@supabase/supabase-js`, reading `SUPABASE_SERVICE_ROLE_KEY` (never `NEXT_PUBLIC_`-prefixed), with `auth: { autoRefreshToken: false, persistSession: false }`. The FIRST line of this file MUST be `import 'server-only'` so an accidental client-bundle import fails the build.
  </action>
  <verify>
    <automated>npx tsc --noEmit && head -1 lib/supabase/service-role.ts | grep -q "server-only" && grep -q "createServerClient" lib/supabase/server.ts && grep -q "createBrowserClient" lib/supabase/client.ts && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0
    - `lib/supabase/service-role.ts` line 1 is `import 'server-only'`
    - `lib/supabase/service-role.ts` references `SUPABASE_SERVICE_ROLE_KEY` and does NOT contain the substring `NEXT_PUBLIC_SUPABASE_SERVICE_ROLE_KEY`
    - `lib/supabase/server.ts` uses `createServerClient` and the `cookies()` getAll/setAll bridge
    - `lib/supabase/client.ts` uses `createBrowserClient` and does NOT import `server-only`
  </acceptance_criteria>
  <done>Three Supabase client factories exist, each correctly scoped to its trust boundary, typechecking clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server code → client bundle | Service-role key must never cross into browser-shipped code |
| npm registry → local install | Third-party package supply chain |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | service-role client / env vars | mitigate | `import 'server-only'` at top of service-role.ts; env key never `NEXT_PUBLIC_`-prefixed; verified by grep gate in Task 2 |
| T-01-SC | Tampering | npm installs (next, @supabase/*, zod, vitest) | accept | RESEARCH.md Package Legitimacy Audit already ran slopcheck on every package — all OK/Approved, zero [ASSUMED]/[SUS]/[SLOP]; no blocking checkpoint required |
</threat_model>

<verification>
- `npx next build` succeeds
- `npx tsc --noEmit` succeeds
- `npx eslint .` clean
- `npx vitest run --passWithNoTests` exits 0
- `supabase/config.toml` present
</verification>

<success_criteria>
Next.js 16 App Router project scaffolded, locked dependencies installed at verified versions, local Supabase initialised, Vitest runnable, and three trust-boundary Supabase client factories created and typechecking clean.
</success_criteria>

<output>
Create `.planning/phases/01-foundation-data-model/01-01-SUMMARY.md` when done.
</output>
</content>
