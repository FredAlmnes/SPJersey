---
phase: 02-order-builder-storefront
plan: 03
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - components/storefront/league-team-select.tsx
  - components/storefront/season-display.tsx
  - components/storefront/size-selector.tsx
  - components/storefront/size-guide-modal.tsx
autonomous: true
requirements: [PROD-01, PROD-02, PROD-06]
must_haves:
  truths:
    - "Customer can pick a league OR 'Landslag' in the first selector; the team selector then lists exactly that group's teams (PROD-01)"
    - "Season renders as a read-only 'Sesong: 2025/26' label (not a dropdown), appearing once a team is chosen"
    - "Customer can select one of six adult sizes S/M/L/XL/XXL/3XL (PROD-02)"
    - "A 'Se storrelsesguide' trigger opens a modal with a placeholder S-3XL measurement table and disclaimer copy (PROD-06)"
  artifacts:
    - path: "components/storefront/league-team-select.tsx"
      provides: "cascading league/Landslag -> team controlled selects"
      exports: ["LeagueTeamSelect"]
    - path: "components/storefront/season-display.tsx"
      provides: "read-only CURRENT_SEASON label"
      exports: ["SeasonDisplay"]
    - path: "components/storefront/size-selector.tsx"
      provides: "6-button adult size group"
      exports: ["SizeSelector"]
    - path: "components/storefront/size-guide-modal.tsx"
      provides: "native dialog size guide with placeholder table"
      exports: ["SizeGuideModal"]
  key_links:
    - from: "components/storefront/league-team-select.tsx"
      to: "lib/cart/team-options.ts"
      via: "getTeamOptions(leagueId) in useMemo"
      pattern: "getTeamOptions"
    - from: "components/storefront/season-display.tsx"
      to: "config/leagues-teams-seasons.ts"
      via: "CURRENT_SEASON import"
      pattern: "CURRENT_SEASON"
---

<objective>
Build the four "what jersey" selection components: the cascading league/Landslag -> team selects, the read-only season label, the six-button size selector, and the native-dialog size-guide modal. These are controlled, stateless-where-possible components that the configurator form (Plan 06) composes; they read Plan 01's getTeamOptions and Phase 1's static catalog, and never own cart state themselves.

Purpose: Delivers the customer-visible PROD-01 (league/team/season), PROD-02 (size), and PROD-06 (size guide) controls. Runs in parallel with Plans 04 and 05 (no shared files).
Output: components/storefront/{league-team-select,season-display,size-selector,size-guide-modal}.tsx.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-order-builder-storefront/02-RESEARCH.md
@.planning/phases/02-order-builder-storefront/02-PATTERNS.md
@.planning/phases/02-order-builder-storefront/02-UI-SPEC.md
@config/leagues-teams-seasons.ts

<interfaces>
<!-- Contracts this plan CREATES (props). Consumed by configurator-form.tsx (Plan 06). -->
<!-- Types imported FROM Plan 01: JerseySize from @/lib/cart/cart-types; getTeamOptions, LANDSLAG_ID from @/lib/cart/team-options -->

components/storefront/league-team-select.tsx:
  interface LeagueTeamSelectProps {
    leagueId: string | null;   // a LEAGUES id | LANDSLAG_ID | null
    teamId: string | null;
    onLeagueChange: (leagueId, leagueName) => void;  // parent resets team
    onTeamChange: (teamId, teamName) => void;
  }
  function LeagueTeamSelect(props)   // "use client"
  // First select: options = LEAGUES (by name) + synthetic { id: LANDSLAG_ID, name: "Landslag" }.
  // Second select: disabled until leagueId set; options = getTeamOptions(leagueId) (useMemo).

  function SeasonDisplay()   // renders "Sesong: {CURRENT_SEASON}" - no props, no state

  interface SizeSelectorProps { value: JerseySize | null; onChange: (s: JerseySize) => void }
  function SizeSelector(props)   // "use client"; 6 buttons S/M/L/XL/XXL/3XL

  function SizeGuideModal()   // "use client"; self-contained trigger button + dialog
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Cascading league/team selects + read-only season label</name>
  <files>components/storefront/league-team-select.tsx, components/storefront/season-display.tsx</files>
  <read_first>
    - lib/cart/team-options.ts (getTeamOptions, LANDSLAG_ID - from Plan 01)
    - config/leagues-teams-seasons.ts (LEAGUES/NATIONAL_TEAMS/CURRENT_SEASON/Team shape)
    - .planning/phases/02-order-builder-storefront/02-RESEARCH.md (Pattern 2 cascading select; season is read-only text, not a dropdown)
    - .planning/phases/02-order-builder-storefront/02-PATTERNS.md (league-team-select + season-display sections; select styling falls back to login input classes)
    - .planning/phases/02-order-builder-storefront/02-UI-SPEC.md (labels "Liga eller landslag"/"Lag"; season read-only; 2-weight rule - NO font-medium; accent only on selected controls)
    - app/(admin)/admin/login/page.tsx (labeled-control Tailwind convention to adapt)
  </read_first>
  <action>
    Create components/storefront/league-team-select.tsx ("use client") exporting LeagueTeamSelect with the LeagueTeamSelectProps contract. First select labelled "Liga eller landslag": render each LEAGUES entry (option value = league.id, text = league.name) plus one synthetic { id: LANDSLAG_ID, name: "Landslag" } option; on change call onLeagueChange(id, name). Derive team options with useMemo(() => getTeamOptions(leagueId), [leagueId]). Second select labelled "Lag": disabled when leagueId === null, options from the memoized team list; on change resolve the chosen team's name from that list and call onTeamChange(id, name). Style selects with the repo input convention (rounded-md border border-zinc-300 px-3 py-2 text-sm dark:border-zinc-700 dark:bg-zinc-900); labels use regular weight (NO font-medium). Header comment referencing D-14/D-15/PROD-01.
    Create components/storefront/season-display.tsx exporting SeasonDisplay - a stateless label rendering "Sesong: <CURRENT_SEASON>" (import CURRENT_SEASON from @/config/leagues-teams-seasons), emphasized value in font-semibold (NOT font-medium), label size (14px/400). No dropdown. Do NOT modify config/leagues-teams-seasons.ts.
  </action>
  <verify>
    <automated>grep -q "getTeamOptions" components/storefront/league-team-select.tsx && grep -q "CURRENT_SEASON" components/storefront/season-display.tsx && ! grep -Rq "font-medium" components/storefront/league-team-select.tsx components/storefront/season-display.tsx && echo COMPONENTS_OK</automated>
  </verify>
  <acceptance_criteria>
    - LeagueTeamSelect renders a "Landslag" option in the first select and disables the team select until a league is chosen
    - Team options are derived via getTeamOptions(leagueId) in a useMemo, not duplicated inline
    - SeasonDisplay shows CURRENT_SEASON as read-only text (no select); config/leagues-teams-seasons.ts is unchanged
    - Neither file uses font-medium (2-weight rule); labels are regular weight
    - npx tsc --noEmit reports no errors for these files
  </acceptance_criteria>
  <done>Cascading league/Landslag -> team selection (PROD-01) and the read-only season label render as controlled components ready for the configurator form.</done>
</task>

<task type="auto">
  <name>Task 2: Six-button size selector</name>
  <files>components/storefront/size-selector.tsx</files>
  <read_first>
    - lib/cart/cart-types.ts (JerseySize type - from Plan 01)
    - .planning/phases/02-order-builder-storefront/02-UI-SPEC.md (Size guide section: 6 options S-3XL; 44px min touch target; accent = selected-state background/ring; 2-weight rule)
    - .planning/phases/02-order-builder-storefront/02-PATTERNS.md (size-selector section - styling convention only, no logic analog)
  </read_first>
  <action>
    Create components/storefront/size-selector.tsx ("use client") exporting SizeSelector with SizeSelectorProps. Render a "Storrelse" label and a group of six button type="button" controls for ["S","M","L","XL","XXL","3XL"] (JerseySize). Each button min 44px touch target (e.g. min-h-11 min-w-11). Selected button (value === size) uses the accent state (bg-emerald-600 text-white dark:bg-emerald-500); unselected uses neutral (border-zinc-300 dark:border-zinc-700). Clicking calls onChange(size). Use font-semibold for selected emphasis if any, never font-medium. Header comment referencing PROD-02/D-19.
  </action>
  <verify>
    <automated>grep -Eq "3XL" components/storefront/size-selector.tsx && grep -q "emerald" components/storefront/size-selector.tsx && ! grep -q "font-medium" components/storefront/size-selector.tsx && echo SIZE_OK</automated>
  </verify>
  <acceptance_criteria>
    - Exactly six size options S, M, L, XL, XXL, 3XL rendered as buttons (no kids' sizes)
    - Selected size uses the emerald accent; unselected stays neutral
    - Interactive controls meet the 44px minimum touch target
    - No font-medium used
    - npx tsc --noEmit clean for this file
  </acceptance_criteria>
  <done>Customer can select one of six adult sizes (PROD-02) via an accessible, accent-highlighted button group.</done>
</task>

<task type="auto">
  <name>Task 3: Native dialog size-guide modal</name>
  <files>components/storefront/size-guide-modal.tsx</files>
  <read_first>
    - .planning/phases/02-order-builder-storefront/02-RESEARCH.md (Code Examples native dialog size-guide modal; Don't Hand-Roll Modal; Open Question 2 placeholder measurements)
    - .planning/phases/02-order-builder-storefront/02-UI-SPEC.md (Size guide section: table columns "Storrelse"/"Brystvidde (cm)"/"Lengde (cm)"; disclaimer copy; lucide info icon trigger; card surface convention)
    - .planning/phases/02-order-builder-storefront/02-PATTERNS.md (size-guide-modal section - no analog, use RESEARCH example verbatim + repo card surface classes)
  </read_first>
  <action>
    Create components/storefront/size-guide-modal.tsx ("use client") exporting SizeGuideModal. Self-contained: a trigger button type="button" labelled "Se storrelsesguide" with a lucide-react Info icon (import { Info } from "lucide-react") that calls dialogRef.current?.showModal(), plus a native dialog with ref={dialogRef} (use useRef<HTMLDialogElement>). Dialog content: an h2 "Storrelsesguide" (20px/600), a placeholder table with columns "Storrelse", "Brystvidde (cm)", "Lengde (cm)" and rows for S/M/L/XL/XXL/3XL using the RESEARCH Open-Question-2 placeholder values (S 96/70, M 100/72, L 104/74, XL 110/76, XXL 116/78, 3XL 122/80), the exact disclaimer paragraph "Forelopig storrelsesguide - kan bli oppdatert senere." (use the exact accented UI-SPEC copy), and a "Lukk" button calling dialogRef.current?.close(). Dialog surface uses repo card classes (rounded-lg border border-zinc-200 p-6 backdrop:bg-black/50 dark:border-zinc-800 dark:bg-zinc-950). Do NOT hand-roll an overlay/focus-trap - the native dialog provides it. Header comment referencing D-17/D-18/PROD-06.
  </action>
  <verify>
    <automated>grep -q "showModal" components/storefront/size-guide-modal.tsx && grep -q "st.rrelsesguide" components/storefront/size-guide-modal.tsx && grep -q "lucide-react" components/storefront/size-guide-modal.tsx && ! grep -q "font-medium" components/storefront/size-guide-modal.tsx && echo MODAL_OK</automated>
  </verify>
  <acceptance_criteria>
    - Trigger button opens the modal via native dialog showModal(); "Lukk" button closes via close()
    - Placeholder table has the three columns and all six size rows, with the exact disclaimer copy from the UI-SPEC contract
    - Uses a lucide-react icon and the native dialog element (no hand-rolled overlay/focus-trap)
    - No font-medium used
    - npx tsc --noEmit clean for this file
  </acceptance_criteria>
  <done>Customer has a size guide (PROD-06): a modal opened from beside the size selector, showing a clearly-labeled placeholder measurement table.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| customer selection -> cart draft | League/team/size chosen client-side; no server trust this phase |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | Tampering | cascading select combinations | mitigate | Invalid league/team combos are structurally impossible - team options are always derived from getTeamOptions(leagueId); season is a single fixed value rendered read-only (no free input) |
| T-02-05 | Information Disclosure | size-guide placeholder data | accept | Placeholder measurements are invented, clearly disclaimed in-UI (D-17); no real supplier data exposed, no runtime attack surface |
</threat_model>

<verification>
- npx tsc --noEmit clean for all four component files
- grep confirms no font-medium in any storefront component in this plan
- grep confirms league-team-select uses getTeamOptions and season-display uses CURRENT_SEASON
</verification>

<success_criteria>
The four selection components render the customer-facing controls for PROD-01 (league/team/read-only season), PROD-02 (size), and PROD-06 (size guide modal), as controlled components matching the UI-SPEC visual/typography/color contract, ready for the configurator form to compose.
</success_criteria>

<output>
Create `.planning/phases/02-order-builder-storefront/02-03-SUMMARY.md` when done.
</output>
