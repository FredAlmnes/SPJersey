---
phase: 01-foundation-data-model
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - config/pricing-tiers.ts
  - config/pricing-tiers.test.ts
  - config/leagues-teams-seasons.ts
  - config/leagues-teams-seasons.test.ts
  - config/patches.ts
  - config/patches.test.ts
autonomous: true
requirements: [ADMIN-01]
must_haves:
  truths:
    - "getOrderTotalOre(3) returns 87000 (3 × 290 NOK whole-order pricing per D-07, not marginal)"
    - "getUnitPriceOre returns 35000/32000/29000 øre for qty 1/2/3+ (D-06: 3-tier bundle discount by quantity)"
    - "The catalog exposes exactly 5 leagues (Premier League, Eliteserien, LaLiga, Serie A, Bundesliga), each with full rosters — D-02: all teams selectable, not a curated subset"
    - "National teams include Norway plus major football nations (D-03: e.g. Brazil, France, Germany, Spain, England, Argentina)"
    - "Patch options include Ligamerke, Champions League, Europa/Conference League, and Ingen"
    - "CURRENT_SEASON is a single exported constant referenced by the catalog, not duplicated per team"
  artifacts:
    - path: "config/pricing-tiers.ts"
      provides: "single source of truth for bundle pricing (D-05..D-08)"
      exports: ["PRICING_TIERS", "getUnitPriceOre", "getOrderTotalOre"]
    - path: "config/leagues-teams-seasons.ts"
      provides: "static league/team/season catalog (D-01..D-04)"
      exports: ["LEAGUES", "NATIONAL_TEAMS", "CURRENT_SEASON"]
    - path: "config/patches.ts"
      provides: "fixed patch list (D-09..D-10)"
      exports: ["PATCHES"]
  key_links:
    - from: "config/leagues-teams-seasons.ts"
      to: "CURRENT_SEASON constant"
      via: "single exported constant referenced by teams"
      pattern: "CURRENT_SEASON"
---

<objective>
Define the static, loadable catalog and pricing configuration the storefront (Phase 2) and the server-side payment recomputation (Phase 3) both depend on: the bundle pricing tiers as the single server-importable source of truth (D-05..D-08), the league/team/season catalog (D-01..D-04), and the fixed patch list (D-09..D-10) — each with unit tests locking the exact decision values.

Purpose: Satisfies Phase 1 success criterion #4 (static catalog + pricing-tier config defined and loadable). `config/pricing-tiers.ts` in particular must be importable from BOTH a Client Component (Phase 2 live summary) and a Route Handler (Phase 3 authoritative recomputation) with no server-only dependency — so it can never be trusted-from-client (D-08).
Output: Three plain-TypeScript config modules with unit tests asserting every locked decision value.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-data-model/SKELETON.md
@.planning/phases/01-foundation-data-model/01-CONTEXT.md
@.planning/phases/01-foundation-data-model/01-RESEARCH.md
@.planning/phases/01-foundation-data-model/01-PATTERNS.md

<interfaces>
<!-- pricing-tiers.ts is a fully verbatim, already-validated excerpt (PATTERNS.md lines 172-190). Use as-is. -->
config/pricing-tiers.ts:
  PRICING_TIERS = [ {minQty:1,maxQty:1,unitPriceOre:35000}, {minQty:2,maxQty:2,unitPriceOre:32000}, {minQty:3,maxQty:Infinity,unitPriceOre:29000} ]
  getUnitPriceOre(quantity: number): number
  getOrderTotalOre(quantity: number): number     // whole-order: getUnitPriceOre(q) * q  (D-07)

<!-- catalog shape (PATTERNS.md lines 201-224) -->
config/leagues-teams-seasons.ts:
  CURRENT_SEASON = '2025/26'  (single constant, referenced — never inline-duplicated; Pitfall 4)
  interface Team { id: string; name: string }
  interface League { id: string; name: string; teams: Team[] }
  LEAGUES: League[]           // D-01: Premier League, Eliteserien, LaLiga, Serie A, Bundesliga; D-02: full rosters
  NATIONAL_TEAMS: Team[]      // D-03: Norway + major football nations
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pricing-tier source of truth + tests</name>
  <files>config/pricing-tiers.ts, config/pricing-tiers.test.ts</files>
  <behavior>
    - getUnitPriceOre(1) === 35000 ; getUnitPriceOre(2) === 32000 ; getUnitPriceOre(3) === 29000 ; getUnitPriceOre(10) === 29000
    - getOrderTotalOre(1) === 35000 ; getOrderTotalOre(2) === 64000 ; getOrderTotalOre(3) === 87000 (whole-order, NOT 35000+32000+29000=96000)
    - getUnitPriceOre(0) throws (no tier)
  </behavior>
  <read_first>
    - .planning/phases/01-foundation-data-model/01-PATTERNS.md (verbatim pricing-tiers.ts excerpt lines 172-190 and the D-08 constraint note lines 192-193 — no `server-only` import here)
    - .planning/phases/01-foundation-data-model/01-CONTEXT.md (D-05 base 350 NOK, D-06 tiers, D-07 whole-order pricing, D-08 single source of truth)
  </read_first>
  <action>
    Create `config/pricing-tiers.ts` exactly per the PATTERNS.md verbatim excerpt: `PRICING_TIERS` array (35000/32000/29000 øre for qty 1 / 2 / 3+ with `maxQty: Infinity` on the top tier), `getUnitPriceOre(quantity)` (finds the tier, throws on no match), and `getOrderTotalOre(quantity)` returning `getUnitPriceOre(quantity) * quantity` (whole-order pricing per D-07). Do NOT add `import 'server-only'` — this module must be importable from client and server (D-08).
    Create `config/pricing-tiers.test.ts` (Vitest) asserting every value in the `<behavior>` block, including the 3-jersey whole-order total of 87000 (explicitly NOT the marginal 96000) and the throw on quantity 0.
  </action>
  <verify>
    <automated>npx vitest run config/pricing-tiers.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `config/pricing-tiers.ts` exports `PRICING_TIERS`, `getUnitPriceOre`, `getOrderTotalOre`
    - File does NOT contain `server-only`
    - Test asserts `getOrderTotalOre(3) === 87000` and `getUnitPriceOre(10) === 29000`
    - `npx vitest run config/pricing-tiers.test.ts` passes
  </acceptance_criteria>
  <done>Bundle pricing is a client-and-server-importable single source of truth with tests locking all D-05..D-08 values.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: League/team/season catalog + patch list + tests</name>
  <files>config/leagues-teams-seasons.ts, config/leagues-teams-seasons.test.ts, config/patches.ts, config/patches.test.ts</files>
  <behavior>
    - LEAGUES has length 5 with names Premier League, Eliteserien, LaLiga, Serie A, Bundesliga
    - Every league has a non-empty teams array
    - NATIONAL_TEAMS includes an entry named "Norge"/"Norway" and at least Brazil, France, Germany, Spain, England, Argentina
    - CURRENT_SEASON is exported and equals '2025/26'
    - PATCHES includes ids/labels for Ligamerke, Champions League, Europa/Conference League, and Ingen (none)
  </behavior>
  <read_first>
    - .planning/phases/01-foundation-data-model/01-PATTERNS.md (catalog required shape lines 201-226; Pitfall-4 constraint that season is a single constant, never inline-duplicated)
    - .planning/phases/01-foundation-data-model/01-CONTEXT.md (D-01..D-04 catalog decisions; D-09..D-10 patches, included in base price, no upcharge)
    - .planning/phases/01-foundation-data-model/01-RESEARCH.md (Pitfall 4 lines 351-355)
  </read_first>
  <action>
    Create `config/leagues-teams-seasons.ts` exporting: `CURRENT_SEASON = '2025/26'` (single `const`), the `Team` and `League` interfaces, `LEAGUES: League[]` populated with the 5 leagues (D-01) and their full current rosters (D-02 — all clubs in each league; use the 2025/26 season rosters), and `NATIONAL_TEAMS: Team[]` with Norway plus major World-Cup/Euro-caliber nations (D-03: at minimum Brazil, France, Germany, Spain, England, Argentina, plus similar). Season must be referenced via the `CURRENT_SEASON` constant only — never a season string duplicated per team (Pitfall 4). Only the current season is modelled (D-04).
    Create `config/patches.ts` exporting `PATCHES` — a fixed list with stable `id` + Norwegian `label` for: Ligamerke, Champions League-merke, Europa League/Conference League-merke, and Ingen (none). Patches carry no price field (included in base price, D-10).
    Create `config/leagues-teams-seasons.test.ts` and `config/patches.test.ts` (Vitest) asserting every value in the `<behavior>` block, including `LEAGUES.length === 5`, that no team object inlines a season string, and that `PATCHES` contains an "Ingen" entry.
  </action>
  <verify>
    <automated>npx vitest run config/leagues-teams-seasons.test.ts config/patches.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `LEAGUES.length === 5` with the exact five league names
    - `CURRENT_SEASON` exported and equal to `'2025/26'`; grep confirms it appears as one `const`, and season is not hardcoded per-team
    - `NATIONAL_TEAMS` includes Norway and the six named nations
    - `PATCHES` includes an "Ingen" option and the three merke options; no price field on patches
    - `npx vitest run` on both files passes
  </acceptance_criteria>
  <done>Loadable league/team/season catalog and patch list defined per D-01..D-04 and D-09..D-10, with tests locking shape and the current-season-constant invariant.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client-submitted total → server pricing | Phase 3 must recompute price from this module, never trust a client total |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | pricing source of truth | mitigate | Pricing lives in one client-and-server-importable module (D-08) so Phase 3 recomputes server-side; whole-order math locked by tests |
| T-03-02 | Tampering | catalog season drift | accept | Static config, low churn; single `CURRENT_SEASON` constant makes annual rollover a one-line edit (Pitfall 4) — no runtime attack surface |
</threat_model>

<verification>
- `npx vitest run config/*.test.ts` all green
- `npx tsc --noEmit` clean
- grep confirms `config/pricing-tiers.ts` has no `server-only` import
</verification>

<success_criteria>
Static catalog (5 leagues + national teams + current season), fixed patch list, and single-source-of-truth pricing tiers are defined as loadable TypeScript modules with unit tests asserting every locked decision value. Phase 1 success criterion #4 satisfied.
</success_criteria>

<output>
Create `.planning/phases/01-foundation-data-model/01-03-SUMMARY.md` when done.
</output>
